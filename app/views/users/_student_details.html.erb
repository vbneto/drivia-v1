<div id="student_detail" class="span">
  <% if current_user.is_parent? %>
    <div id="student_detail" class="span">
      <b>
        <%= t('users.student_parent_dashboard.display_data_of_student') %>
        <%= @parent_students.first.student_name%>
      </b></br>
      <b>
        From <%= select_default_start_month %> <%= set_selected_year %>
        To <%= select_default_end_month %> <%= set_selected_year %>
      </b></br>
      <b>
        <%= t('users.student_parent_dashboard.school_name') %> 
        <%= @parent_students.first.school.name %>
      </b></br>
      <b>
         <%= t('users.student_parent_dashboard.grade_name') %> 
         <%= @parent_students.first.current_grade %>
      </b>  
    </div>
  <% end %>
  <% if current_user.is_student? || current_user.is_school_administration? %>
    <div id="student_detail" class="span">
      <b><%= t('users.student_parent_dashboard.display_data_of_student') %> <%= @student.student_name%></b></br>
      <b>
        From <%= select_default_start_month %> <%= set_selected_year %>
        To <%= select_default_end_month %> <%= set_selected_year %>
      </b></br>
      <b><%= t('users.student_parent_dashboard.school_name') %> <%=@student_school_status.school.name %></br>
      <b><%= t('users.student_parent_dashboard.grade_name') %> <%= @student_school_status.current_grade %></b>  
    </div>
  <% end %>
</div>
